[GLOBAL]
# Scale the entire rendering (cells, margins, text, stroke, shadow)
render_scale = 4.0          ; 1.0 = current size, 2.0 = 2× pixels in each dimension
# Embed print DPI metadata
output_dpi = 300
# JPEG tuning for crisp text (if saving .jpg/.jpeg)
jpeg_quality = 95
jpeg_subsampling = 0         ; 0=4:4:4 (no chroma subsampling), best for text
# For PNG (optional):
png_compress_level = 6

# Grid: set rows/cols below.
rows = 2
cols = 2

# Pixels between cells (also used as outer margin).
margin = 18

# Canvas cell size in pixels (each image fits within this area).
cell_width = 800
cell_height = 600

# File to save (png/jpg). PNG recommended for sharp text.
output_file = collage_output.png

# Background color for canvas (hex or rgb/rgba).
background_color = #ffffff

# ----- Text defaults (can be overridden per image) -----
text_string = Test                ; (optional; if empty and not overridden, no text drawn)
text_color = #ffffffff
text_size = 32
# Either a system font file path OR leave blank to auto-pick a common font.
text_font =                   ; e.g., C:\Windows\Fonts\arial.ttf or /usr/share/fonts/dejavu/DejaVuSans.ttf
text_alignment = left         ; left | center | right
text_position = top-left      ; Vertical: top/middle/bottom + Horizontal: left/center/right (hyphenated) Example: top-left, middle-center
text_padding = 12             ; padding (px) from the chosen anchor edge(s)
text_box_max_width_pct = 0.9  ; wrap text to a fraction of cell width (0-1)

# Make text pop on any background
text_stroke_width = 2
text_stroke_color = black
text_shadow = 1
text_shadow_offset = 3,3
text_shadow_color = rgba(0,0,0,0.65)
text_shadow_radius = 1

# If you want to dim images to make text pop (0-255). 0 = no overlay; 255 = black.
dim_overlay_alpha = 0

# Option to draw red rectangles around the text boxes for debugging
# debug_caption_boxes = 1

[IMAGES]
# Order defines placement row-major (top-left to right, then next row), or add row/col in per-image section.
# List only the filenames found under ./photos/ or subdirectories
order =
    img1.svg
    img2.svg
    img3.svg
    img4.svg

# -------- OPTIONAL: Per-image overrides ----------
[image:img1.svg]
text_string = a)
# text_position = middle-left      ; top/middle/bottom + left/center/right (hyphenated)
# text_alignment = left         ; left | center | right
# text_color = #ff0055
# text_size = 42
# text_font = /usr/share/fonts/dejavu/DejaVuSans-Bold.ttf
# row = 1
# col = 2  ; 1-based indexing for explicit placement (overrides order list)

[image:img2.svg]
text_string = b)

[image:img3.svg]
text_string = c)

[image:img4.svg]
text_string = d)